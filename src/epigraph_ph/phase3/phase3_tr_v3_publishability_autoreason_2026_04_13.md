# Phase 3 TR-V3 Publishability Autoreason Addendum

**Date:** 2026-04-13  
**Method:** bounded `A / B / AB` arbitration after council critique round  
**Question:** What is the strongest publishable direction for the current Phase 3 TR-V3 work?

---

## 1. Arbitration Inputs

### A: Incumbent chairman synthesis

Main paper:

- provenance-aware, contract-sensitive quarterly epidemic forecasting under mixed evidence

Secondary tracks:

- mechanistic negative-result / identifiability paper
- broader benchmark-contract methods paper

### B: Strongest revision

Main paper:

- observation-first quarterly stock/flow forecasting wins under honest contracts

Secondary tracks:

- mixed-evidence contract design as supporting methods layer
- mechanistic branch only as a short negative-result appendix

### AB: True synthesis candidate

Main paper:

- provenance-aware, contract-sensitive forecasting under mixed evidence

Empirical centerpiece:

- observation-first stock/flow forecasting wins under both exact and dense contracts

Secondary track:

- constrained mechanistic branch as negative or follow-up result

---

## 2. Evaluation Gate

Judge `A`, `B`, and `AB` against:

1. factual grounding in current artifacts
2. boundedness
3. actionability for the next autoresearch batch
4. fit to the current benchmark/evidence gate
5. avoidance of fake novelty

---

## 3. Comparison

| Candidate | Strengths | Weaknesses | Decision read |
|---|---|---|---|
| `A` | Best fit to the evidence contract story; strongest methodological defensibility; already aligned with purged dense and endpoint/tier audits | Risks sounding abstract if the empirical hook is underplayed | Strong |
| `B` | Strongest empirical hook; easiest story for the current winners; immediately legible to forecasting readers | Too narrow; more vulnerable to "local forecasting note" criticism; underuses the provenance contribution | Plausible but weaker |
| `AB` | Keeps the methodological strength of `A` while making the empirical hook explicit; matches current artifact structure well | Very close to `A`; must avoid bloating into two papers at once | Strongest if kept tight |

---

## 4. Winner

**Winning artifact:** `AB`

This is a real synthesis, not a cosmetic rewrite.

The key improvement over `A` is that it forces the paper backbone to be:

- a provenance-aware, contract-sensitive benchmark paper

while also making the central empirical fact impossible to miss:

- the observation-first `R10` family is the only live predictive frontier under the current archive

`B` loses because it is too easy to dismiss as a local forecasting note.  
`A` loses narrowly because it can sound more abstract than the actual evidence base.  
`AB` wins because it keeps the benchmark/provenance novelty while anchoring it to a strong empirical model-comparison result.

---

## 5. Strongest Claim Retained

The strongest retained claim is:

- under mixed, provenance-tagged epidemic surveillance evidence, benchmark contract design and evidence provenance materially change what "best model" means, and under honest blocked-time evaluation an observation-first stock/flow forecaster outperforms the current mechanistic hidden-state branch.

---

## 6. Strongest Claim Revised Or Removed

Removed:

- any implication that the current mechanistic `TR-V3` branch is the fastest path to a publishable main paper

Revised:

- the mechanistic branch is still scientifically useful, but it is now a secondary comparator or follow-up paper, not the main submission backbone

---

## 7. Why `AB` Beat The Alternatives

`AB` won because it scores best on all five gates:

- **factual grounding:** exactly matches the current artifact frontier
- **boundedness:** uses existing evidence instead of demanding a whole new model family
- **actionability:** translates directly into a frozen benchmark package and a small next experiment batch
- **fit to gate:** aligned with exact-only, purged dense, and endpoint-tier audit logic
- **fake novelty avoidance:** does not pretend the current winner is already a mechanistic epidemic generator

---

## 8. Stop Or Continue

**Decision:** stop

The council has converged enough. More arbitration would likely just rename the same plan.

The next step is execution, not more ideation.

---

## 9. Exact AutoResearch Handoff

**Variant:** `evidence-to-model-loop`

**Winning plan:** `AB`

**Evaluation harness:**

- exact-only frozen contract
- legacy dense contract
- purged dense contract
- one untouched lockbox split
- per-metric raw MAE
- per-tier scoring
- support counts
- suppression honesty flags
- calibration and uncertainty package

**Mutation units:**

- frozen contract audit package
- lockbox creation
- winner calibration/uncertainty report
- exact-lane refinement validation for `R10-M1`
- dense-lane suppression-honesty repair
- dense-lane transferred `M1` consistency test
- dense-lane train-only bias correction
- one constrained mechanistic comparator only

**First experiments:**

1. frozen contract audit package on `R10-EXACT`, `R10-DENSE`, `R10-M1`, `R1`, and carry-forward
2. lockbox split with no further tuning access
3. calibration/uncertainty package for `R10-M1` and `R10-DENSE`
4. dense champion suppression-honesty repair
5. dense champion plus transferred `M1`-style consistency constraint
6. dense champion plus train-only bias correction
7. one constrained mechanistic comparator for the discussion section

**Stop rule:**

- keep only if the frozen benchmark or lockbox improves without honesty regression
- otherwise revert and write the paper around the already defensible frontier

