# Goal

Build a Philippines-specific transition model family that beats simple baselines under blocked-time evaluation.

The first target in this sandbox is `TR-V3-00`:
- no Phase 2 priors yet
- replace carry-forward with a train-only dynamic hazard model
- validate on frozen blocked-time splits from `smoke-latent-blocks`
